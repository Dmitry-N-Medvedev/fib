/* eslint-env node */

const {
  describe,
  it,
} = require('mocha');
const {
  expect,
} = require('chai');

describe('Fibonacci Sequence', () => {
  it('should calculate Nth position', () => {

  });
});
